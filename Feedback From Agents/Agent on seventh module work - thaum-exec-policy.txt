Framework feedback — implementing-session #7 (thaum-exec-policy)
Author: implementing thread (single shot, no carryover from prior sessions)
Date: 2026-05-03
Module: thaum-exec-policy (7th of the post-pipeline implementation modules; replaces ExecPolicySeam.allowAll() v0 stub from D210)

Top-line: framework continued to do its job at this maturity level. Three CONVENTIONS load-bearing reads (C2 tripwire vocabulary, C4 verbatim spec quotes, C6 lift-or-not) directly shaped output; the orchestrator's calibration-by-precedent target (thaum-tools closeout, ~108 lines / 8 H2 sections) gave the right shape signal without me having to negotiate "what does done look like." The §4 explicit-design-question section in the prompt produced six clean decisions (D224–D229 mapping 1:1 to §4.1–§4.7); those are exactly the entries the orchestrator will use to draft the next prompt.

Specific signal-level observations:

1. The "YOU MUST CLOSE THESE" framing for the §4 design questions is the highest-yield part of the prompt. Without that explicit framing, I'd probably have made some of these calls implicitly and left the rationale undocumented. The closeout's "Decisions Beyond Prompt" section is the *artifact*; the prompt's explicit-question framing is what *produces* it. Strongly recommend keeping this pattern in every module-implementation prompt going forward — it's the difference between "here's what I built" and "here's what I built AND why I made every load-bearing call."

2. CONVENTIONS.md C4 (verbatim spec quotes) is tested by the implementation, not by review. I dropped the §Required Behaviors block + the 5-clause fail-closed contract above loadExecPolicy() and the act of typing them out flagged that clauses 3 ("aggregate errors before refusal") and 4 ("Forbidden default") needed *separate code* — clause 3 in the loader, clause 4 in the checker. Without the verbatim block I might have implemented clause 3 inside checkPolicy ("if any rule failed to parse, default-Forbidden"), which would have been wrong. The dead-man's switch caught the mis-decomposition at write-time, not at review time. Concrete win for C4; suggest framework documentation cite this case ("the convention catches errors *during* implementation, not just after").

3. The "second occurrence" signal for Result<T,E> (D232) is C6 working as intended. The prompt explicitly flagged it ("flag in your closeout") and the resulting D232 entry hands the orchestrator a clean lift-or-not decision. Two notes:
   (a) I had to RESIST the temptation to lift unilaterally. Convention says "don't lift; document and let the orchestrator decide" — and circumstantial pressure ("just lift it now, save a future session's work") would have been the wrong call here because the lift forces a circular-dep angle: thaum-tools/bash.ts will import thaum-exec-policy soon, so if both modules import Result from thaum-tools, thaum-exec-policy ends up as a dep of thaum-tools AND a dependent of thaum-tools. The cleanest answer (lift to thaum-protocol) requires orchestrator coordination because it touches two existing modules; one implementing session can't responsibly do that. C6 + the prompt's signal-not-act discipline kept this clean.
   (b) The prompt could explicitly note "third occurrence triggers a lift recommendation," which it does, but maybe also note "second occurrence + circular-dep makes the lift candidate stronger" — that case isn't covered explicitly and almost made me overcorrect.

4. Pre-existing breakage cost ~10 minutes of session time. The stray `st` token + trailing NUL bytes on packages/thaum-engine/src/seams.ts and the stale absolute-path import in _debug-test.ts were left over from a prior session that closed claiming green gates. The "all gates green workspace-wide" rule made these mine to clear, which is correct posture. But: the prior closeout claimed 371 pass / 6 skip / 0 fail at session close — and that count is what showed up *after* I fixed the breakage on this session. The closeout-vs-reality gap suggests the framework should add a "verify entry baseline before scoping" sub-step to the implementing-session preamble — run the 5 gates before starting work, surface any drift, and cap pre-work fixes at "less than 5% of session time" or escalate. The prompt itself helped: the orchestrator's note ("Note the stray `st` token at line ~233 that an earlier session left in seams.ts") was warning me about exactly the problem — I just didn't connect the warning to the test failure fast enough. Suggest: orchestrator-prepared prompts should put pre-existing-breakage warnings *at the top of §1 Read-First* rather than buried in §1 item 9, so they read as "FIX THIS FIRST" not "by the way."

5. The "calibration target as last item read" sequence (§1 item 2: "Calibration target — read in full") is doing useful work. I read the thaum-tools closeout right after the §Discipline block and the shape immediately became concrete: 8-9 H2 sections, each non-trivial, each bullet earning 1-3 sentences of rationale. Without that calibration I'd have written ~50% of the closeout's actual length. C12 is well-tuned.

6. Markup stability: every prompt I've seen has been fine to read in plain text. No issues with the prompt format itself. The orchestrator's "I expect you to refine this prompt rather than rubber-stamp it" line at the bottom of §Final notes is good framing — it shifts implementing-session attention from "do as told" to "co-author the call." That's the orchestrator-implementer collaboration the framework documents.

7. Scope-discipline-as-discipline: §3 IN/OUT was crisp. The temptation to wire the engine call-site (it's a few lines!) was real but the prompt explicitly named it CF-exec-policy-1 + CF-tools-1 — I left it. Producing a clean stopping-point + carry-forward note is more aligned with "next session can pick this up cleanly" than with my instinct to ship more. The prompt's explicit naming of the two carry-forwards ("the natural call-site is packages/thaum-tools/src/bash.ts") gave me language to hand the next session a turn-key kickoff. Good shape.

8. Subagent delegation was not needed for this module. ~600 lines of new src + ~400 lines of tests + a closeout. All work fit in primary context. The framework's subagent-delegation guidance is for codebases ≥50 source files / ≥100K LOC; module-implementation sessions for new packages don't typically hit that bar. No suggestion needed; observation only.

9. Closeout density target hit. The thaum-tools closeout is 108 lines / 8 H2 sections. Mine landed at ~155 lines / 9 H2s — slightly over but the extra came from D232 (C6 second-occurrence) needing more rationale than thaum-tools' D219 did, and from the side-effect cleanups (D234, D235) being necessary backstory. I think that's the right tradeoff — under-document the rationale and the next session re-derives it.

10. Linux container quirk: bun install reports "Failed to install 1 package" but everything resolves. Some optional native binding (oxlint or oxfmt) is failing its cpu-mismatch download in this specific session container. Not blocking — workspace symlinks are missing in node_modules but Bun's bundler resolution finds the workspace packages via name. The prior session (also 2026-05-03 thaum-tools) ran on a different host and reported clean install. Flagging in case it shows up in other sessions.

Net assessment: framework is in a good steady state for this maturity. Marginal improvements are sub-5%-of-session-time signal-strength tweaks, not structural. The conventions + decisions corpus is paying off — D210 told me what `allowAll()` was for, C2 told me to keep it test-only, C4 made me write the verbatim block, C6 told me to flag-not-lift. Every one of those "what should I do here" moments resolved into "the convention/decision says X" rather than "I have to figure this out from scratch." That's the system working.
